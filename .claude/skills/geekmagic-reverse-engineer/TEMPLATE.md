<!--
  TEMPLATE for the polished per-device report.

  The script pre-stages this file as `report.md` in the run dir, so it
  already exists when you arrive. EDIT IT IN PLACE — replace every
  `<!-- AGENT: ... -->` HTML comment with real content, removing the
  comment marker. Drop entire sections (heading + body) that don't apply.

  Source material in the same directory:
  - `report.raw.md`   — full script output (preserved as-is — your appendix).
                        body_preview is capped at 32 KB per endpoint, which
                        comfortably fits the source HTML/JS we've seen.
                        If a page is bigger and you need the rest, ask the
                        user to curl it from the device.
  - `report.json`     — raw sidecar (machine-readable mirror of .raw.md)
  - `run.json`        — run metadata + summary counts (probed_ok, failed,
                        state_changing, runtime_blocked)

  Source material in the repo:
  - `custom_components/geekmagic/device.py`       — HTTP client; what
                                                    `GeekMagicDevice`
                                                    methods call
  - `custom_components/geekmagic/const.py`        — model/theme constants
  - `custom_components/geekmagic/coordinator.py`  — what state is polled

  Goal: a maintainer reading this file should, in under a minute, know
  (a) what the device is, (b) what won't work with the integration today,
  (c) what would need to change to support it. The raw report is the
  appendix; this file is the briefing.

  When the device's host is `<DEVICE>` everywhere (which happens by
  default — network redaction is on), write `<DEVICE>` for the Host row.
  Don't invent an IP.
-->

# <!-- AGENT: friendly device label, e.g. "SmallTV-Ultra V9.0.40" — omit the IP, the user may not want it public -->

> Generated by the [`geekmagic-reverse-engineer`](../../../.claude/skills/geekmagic-reverse-engineer/SKILL.md) skill.
> Raw artifacts in this directory: [`report.raw.md`](report.raw.md) · [`report.json`](report.json) · [`run.json`](run.json).
> Reproduce: `uv run .claude/skills/geekmagic-reverse-engineer/reverse_engineer_device.py <HOST>` (writes a new timestamped subdirectory).

## Device at a glance

|   |   |
|---|---|
| Host | <!-- AGENT: ip or host:port; write `<DEVICE>` if network-redaction was on --> |
| Model | <!-- AGENT: as reported by /v.json or equivalent (e.g. `SmallTV-Ultra`, `GeekMagic SmallTV-PRO`); "unknown — no identification endpoint" if the device exposes none --> |
| Firmware | <!-- AGENT: version string verbatim, or "unknown" --> |
| Web UI title | <!-- AGENT: from <title> of root page; omit the row if empty --> |
| Identification endpoint | <!-- AGENT: which path returned the identity, e.g. `GET /v.json`. If none, write "none — inferred from <evidence>" --> |
| Endpoints discovered | <!-- AGENT: copy the breakdown from run.json's `summary` field, e.g. "40 total — 25 probed OK, 8 4xx/5xx, 7 state-changing, 0 runtime-blocked" --> |

<!-- AGENT: ONLY if `summary.runtime_blocked > 0` in run.json, add a
callout here naming the URL(s) the page tried to fire on load (find them
in report.raw.md's state-changing section, look for the `runtime_blocked`
evidence kind). If the count is 0, DELETE THIS COMMENT and leave no
callout — don't write "no blocked requests" or similar; the absence is
the signal. -->

## Compatibility with this integration

<!-- AGENT: Open these BEFORE writing this section:
  - `custom_components/geekmagic/device.py`
  - `custom_components/geekmagic/const.py`
  - `custom_components/geekmagic/coordinator.py`

For each method on `GeekMagicDevice`, check TWO things, not one:
  1. Does the endpoint it calls exist on this device? (look at report.raw.md
     "JSON endpoints" + "State-changing endpoints" sections)
  2. Does the device return the FIELDS the integration reads? Example:
     `get_state()` reads `data.get("brt")` and `data.get("img")` from
     `/app.json`. If the device returns `/app.json` with only
     `{"theme": N}`, the endpoint matches but the integration will see
     `brt=None` and `img=None` — a silent partial failure that's worse
     than a 404.

Cross-check field names by reading the JSON sample shown inline in
`report.raw.md` for each endpoint, and comparing keys with the
integration's `data.get(...)` calls. -->

### What should work

<!-- AGENT: Bullet list of integration methods whose endpoint AND fields
both line up. e.g.:
- `get_brightness()` — `/brt.json` returns `{"brt": "71"}`; matches the
  `data.get("brt", 0)` in `device.py`
Omit the section if nothing matches. -->

### What likely breaks

<!-- AGENT: Concrete failure predictions with the reason and the source of
the prediction. Cite line numbers in `device.py` where useful. Examples:
- `detect_model()` returns `MODEL_UNKNOWN` — device has no `/.sys/app.json`
  (404) and no `/app.json` (404); only `/v.json` works and reports
  `GeekMagic SmallTV-PRO`. (`device.py:371-404`)
- `get_state()` reads `data.get("brt")` and `data.get("img")` from
  `/app.json`, but this device returns only `{"theme": N}` (see the
  `/app.json` sample in `report.raw.md`). Brightness and current image
  will be `None`. (`device.py:104-107`)
- `set_brightness(N)` writes `/set?brt=0..100`, but this device's
  brightness range is `0..255` — see `min="2" max="99"` on
  `#ledBrightness` in the `/` body_preview of `report.raw.md`.
  (`device.py:156-166`)
- `upload_and_display()` uploads to `POST /doUpload?dir=/image/`, but this
  device's upload endpoint is `POST /upload` (multipart field `imageFile`
  per the `/javascript.js` preview in `report.raw.md`). (`device.py:201-244`)
Omit the section if nothing breaks. -->

### Extra capabilities this device has

<!-- AGENT: Features the device exposes that the integration doesn't
currently surface — stocks, crypto, monitor pages, photo album rotation,
OTA, etc. Reference the source page or endpoint. Omit if nothing notable. -->

## Endpoints by feature

<!-- AGENT: Cluster the discovered endpoints by purpose, not alphabetically.
Use the suggested groups below — omit any group with no endpoints, add new
groups if the device exposes a category we haven't seen before. For each
row, prefer this shape:

- `METHOD /path` — one-line purpose. **Inputs:** params with inferred
  type/range, citing source (e.g. "0–100, from slider min/max in the
  `/settings.html` body_preview"). **Effect:** what changes on the device.

For read-only endpoints, summarise the response shape (key → meaning), not
just the raw schema (the raw report already has the schema). For
state-changing endpoints, semantics usually live in the corresponding JS
function — search the body_preview of the JS file in `report.raw.md`. -->


### Identification & system

<!-- AGENT: /v.json, /space.json, /restart, /reboot, /update_ota, etc. -->

### Display

<!-- AGENT: brightness, night mode, theme switching, colour, etc. -->

### Network / WiFi

<!-- AGENT: /wifi.json, /wifisave, /connect, /scanwifi, etc. -->

### Content & data

<!-- AGENT: weather, time, image/photo, stock, crypto, monitor, etc.
Split into subsections if it gets long. -->

## Things to verify manually

<!-- AGENT: List anything you couldn't determine with confidence:
ambiguous endpoint semantics, parameter type guesses that need a real
hardware test, paths that returned 200 but with empty/unclear bodies,
single-letter JSON keys whose meaning you had to infer, etc. This helps
the maintainer prioritise what to test on real hardware. Omit if nothing. -->

## Privacy review

<!-- AGENT: Auto-redacted by the script: credentials (always), and network
identity (SSIDs, IP addresses, MAC/BSSID — unless --no-network-redact).
List below ONLY things you found that the script DIDN'T catch and either
scrubbed by hand or are leaving in with the user's OK:

- e.g. "City `Bailly-Romainvilliers, FR` left in the `/config` sample —
  user OK with this being public."
- e.g. "OpenWeatherMap key under generic name `apiKey` was missed by the
  default regex; manually replaced with `[REDACTED]` in report.raw.md
  and report.json."

Write "Nothing flagged beyond default redactions." if the audit was clean.
NEVER commit this file (or anything in this dir) with un-acknowledged
secrets in it. -->

---

<!-- AGENT: Delete THIS comment only. The italic line below it (starting
"_Full discovery output:") is the final published footer — keep it as-is. -->
_Full discovery output: see [`report.raw.md`](report.raw.md). To refresh after a firmware update, rerun the skill — a new timestamped subdirectory is created so old captures remain intact for diffing. This file (`report.md`) is human-curated; copy it forward into the new run's directory and update what changed._
